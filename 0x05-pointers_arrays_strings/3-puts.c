#include "main.h"

/**
 * _puts - prints a string to stdout followed by new line
 *@str: pointer to the string to be printed
 */
void _puts(char *str);
{
	int i;

	for (a = 0; str[a] != '\0'; i++)
	{
		_putchar(str[i]);
	}
	_putchar ('\n')
}
